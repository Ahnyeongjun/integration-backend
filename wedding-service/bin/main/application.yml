server:
  port: 8089
spring:
  application:
    name: wedding-service
  datasource:
    url: jdbc:mysql://localhost:3306/wedding_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
jwt:
  secret: your-256-bit-secret-key-for-jwt-token-generation-min-32-chars
